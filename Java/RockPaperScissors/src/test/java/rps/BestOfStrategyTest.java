package rps;

import org.junit.Test;

public class BestOfStrategyTest {
    @Test
    public void shouldGoOnForNThrows() {
        int[] inputs= {5,10,30};
        for (int i = 0; i < inputs.length; i++) {
            BestOfStrategy bestOfStrategy = new BestOfStrategy(inputs[i]);
            //bestOfStrategy.

        }
      //new BestOfStrategy()
    }
}
